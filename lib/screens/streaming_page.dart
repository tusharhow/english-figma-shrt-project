import 'package:flutter/material.dart';

class SreamingPage extends StatelessWidget {
  const SreamingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}